[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pcm-lifecycle-trial"
version = "0.1.0"
description = "Disposable PCM lifecycle trial"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["trial*"]
